"""Not needed once you're connected to the real TOMS database.

This project now runs against your live `toms` data, so there's nothing to
seed. To verify the connection and see collection counts, run:

    py check_db.py
"""

if __name__ == "__main__":
    print(__doc__)
